#include <bits/stdc++.h>
using namespace std;

int main() {
    // int n, s; cin >> n >> s; 
    // vector<int> nums;
    // map<int, int> m;
    // for (int i = 0; i < n; ++i) {
    //     int cur; cin >> cur;
    //     nums.push_back(cur);
    //     m[cur] = i;
    // }
    // for (int i = 0; i < n; ++i) {
    //     if (m.count(s - nums[i]) && m[s - nums[i]] != i) {
    //         cout << i + 1 << ' ' <<  m[s - nums[i]]+ 1 << endl;
    //         return 0; 
    //     }
    // }
    // cout << "IMPOSSIBLE" << endl;
}
